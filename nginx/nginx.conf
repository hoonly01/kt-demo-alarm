# Nginx 리버스 프록시 설정
# FastAPI 애플리케이션용

upstream fastapi_backend {
    server localhost:8000;
}

# HTTP -> HTTPS 리다이렉트 (SSL 인증서 설정 후 주석 해제)
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$server_name$request_uri;
# }

# HTTP 서버 (개발/테스트용)
server {
    listen 80;
    server_name _;  # 모든 도메인 허용 (프로덕션에서는 실제 도메인으로 변경)

    # 클라이언트 요청 크기 제한
    client_max_body_size 10M;

    # 로그 설정
    access_log /var/log/nginx/kt-demo-alarm-access.log;
    error_log /var/log/nginx/kt-demo-alarm-error.log;

    # 헬스체크 엔드포인트
    location /health {
        access_log off;
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }

    # FastAPI 프록시
    location / {
        proxy_pass http://fastapi_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 타임아웃 설정
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # WebSocket 지원 (필요 시)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 정적 파일 (필요 시)
    # location /static/ {
    #     alias /home/ubuntu/kt-demo-alarm/static/;
    #     expires 1d;
    #     add_header Cache-Control "public, immutable";
    # }
}

# HTTPS 서버 (SSL 인증서 설정 후 사용)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     # SSL 인증서 경로 (Let's Encrypt)
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#
#     # SSL 설정
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # 클라이언트 요청 크기 제한
#     client_max_body_size 10M;
#
#     # 로그 설정
#     access_log /var/log/nginx/kt-demo-alarm-ssl-access.log;
#     error_log /var/log/nginx/kt-demo-alarm-ssl-error.log;
#
#     # 헬스체크 엔드포인트
#     location /health {
#         access_log off;
#         return 200 "OK\n";
#         add_header Content-Type text/plain;
#     }
#
#     # FastAPI 프록시
#     location / {
#         proxy_pass http://fastapi_backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         # 타임아웃 설정
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#
#         # WebSocket 지원
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
